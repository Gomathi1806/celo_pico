// Alias route for Base PICO parity — same logic as /api/tools/summarize-url.
// Accepts any URL (article, blog, social thread, etc.) and returns a TL;DR.
export { POST } from "../summarize-url/route";
